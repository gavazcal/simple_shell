#include "holberton.h"

/**
 *find_path - find PATH
 *
 *@environ: environ variable
 *
 *Return: string with PATH, Null if it fails
 */

char *find_path(char **environ)
{
	if (environ == NULL)
		return (NULL);
	
	return (NULL);
}
